/**
 * Nombres de cookies de sesión, en un módulo sin dependencias para que pueda
 * importarlo tanto el código de servidor (admin.ts) como el proxy (edge).
 */
export const ADMIN_COOKIE = "caramba_admin";
